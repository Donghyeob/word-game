import { style } from '@vanilla-extract/css';

export const timeLimitWrapper = style({
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'start',
});

export const timeLimitBox = style({
    border: 'solid 2px #d7dbe6',
});
